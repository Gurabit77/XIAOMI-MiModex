# animal-island-ui

This directory contains the vendored build output of the UI package used by MiModex.

It is included so the public MiModex repository can be installed and built without depending on a local machine path.
